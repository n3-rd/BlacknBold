/** @type {import('tailwindcss').Config} */
module.exports = {
  mode: "jit", // enable tailwind just in time
  purge: ["./index.html"],
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
};